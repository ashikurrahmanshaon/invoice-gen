<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # Exclude specific static files from SPA routing
  RewriteRule ^(sitemap\.xml|robots\.txt|favicon\.ico|manifest\.json)$ - [L]
  
  # Serve physical files or directories if they exist
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  
  # For all other routes (SPA routes), serve index.html
  RewriteRule ^ index.html [QSA,L]
  
  # Handle the case where a physical directory exists but lacks index.html 
  # (e.g. /templates) - route to index.html instead of 403 Forbidden
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteCond %{REQUEST_FILENAME}/index.html !-f
  RewriteRule ^(.*)$ /index.html [L]
</IfModule>
